## What changed

<!-- One or two sentences. -->

## Why

<!-- The problem this solves. If it changes a prompt, say which failure it prevents. -->

## Checks

- [ ] Nothing added here is specific to one codebase (product facts go in `autopilot.config.json`)
- [ ] Any new prompt rule states the failure it exists to prevent
- [ ] Diagrams are inline mermaid, not image files
- [ ] No credentials, real product names, or repo paths outside `schema/example.*.json`
- [ ] If the schema changed, the examples were updated too
- [ ] Relative links resolve

## If this changes a settled decision

<!-- The four settled decisions are in the README. Say what changed in the world,
     not why the trade-off is hard: the alternatives were already in view. -->
